# Code review report for `sample.py`  
**Generated on :** 20260814 153803
**Source file path :** `sample_inputs\sample.py`

----

    Here is my code review for the provided snippet.

---

### 1. Summary
The code defines a function, `calculate_avg`, meant to compute the arithmetic mean of a collection of numbers. Following the definition, it attempts to display the result using `print`.

---

### 2. Potential Bugs & Edge Cases

*   **`ZeroDivisionError` on Empty Input**: If `numbers` is empty (including calling the function with no arguments `calculate_avg()`), `len(numbers)` evaluates to `0`, causing a `ZeroDivisionError: division by zero`.
*   **Mutable Default Argument**: Using `numbers=[]` in the function signature is a well-known Python anti-pattern. Default parameter values are evaluated once when the function is defined, not each time it is called. If the list is ever mutated inside the function in future iterations, those changes will persist across calls.
*   **Function Not Invoked**: The line `print(calculate_avg)` prints the function reference object (e.g., `<function calculate_avg at 0x...>`) rather than executing the function. To execute it, you must pass arguments and call it with parentheses, e.g., `print(calculate_avg([1, 2, 3]))`.
*   **Type Errors**: If `numbers` contains non-numeric types (e.g., strings or incompatible objects), `total += n` will raise a `TypeError`.

---

### 3. Optimization & Style

*   **Use Python Built-ins**: Manually looping to sum numbers is redundant and slower than using Python's built-in `sum()` function or `statistics.mean()`.
*   **PEP 8 Violations**:
    *   **Indentation**: Code uses 4-space indent inconsistently.
    *   **Whitespace around operators**: Missing spaces around assignments and operators (e.g., `total = 0`, `total += n`, `total / len(numbers)`).
*   **Type Hinting & Docstrings**: Adding type hints and a docstring improves readability and enables static type checking.

#### Refactored Code Example

```python
from typing import Sequence


def calculate_avg(numbers: Sequence[float] | None = None) -> float:
    """Calculate the arithmetic mean of a sequence of numbers.

    :param numbers: A sequence of int or float values.
    :return: The average of the numbers.
    :raises ValueError: If the sequence is empty.
    """
    if not numbers:
        raise ValueError("Cannot calculate the average of an empty sequence.")

    return sum(numbers) / len(numbers)


# Example usage:
sample_data = [10, 20, 30]
print(calculate_avg(sample_data))  # Outputs: 20.0
```

---

### 4. Score

**Score: 3/10**

**Justification**: 
The code suffers from multiple fundamental issues: a high-risk default parameter bug (`ZeroDivisionError` on default execution), the mutable default argument anti-pattern, PEP 8 formatting errors, and a missing invocation syntax on the `print` statement. Re-implementing summation instead of using Python's built-in `sum()` also misses standard language idioms.
    